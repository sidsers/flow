{
  "name": "jira-clone",
  "version": "1.0.0",
  "description": "Internal Jira clone — full app (backend + frontend)",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
